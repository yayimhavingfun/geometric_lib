# accepts a - side, returns area of a square
def area(a):
    if a == 0:
        return "a cannot be 0"
    return a * a


# accepts a - side, returns perimeter of a square
def perimeter(a):
    if a == 0:
        return "a cannot be 0"
    return 4 * a
